# Autolink check

Visit https://example.com for the details.

This paragraph is plain prose with a single bare URL above and no
Markdown-syntax link anywhere — so the rendered preview contains a link
ONLY if bare URLs are autolinked.
