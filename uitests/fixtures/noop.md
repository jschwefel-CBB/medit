# No-Op Fixture

This document is opened by `preview-edit-ops-noop.json` to prove that Cut, Paste,
and Delete are inert in the rendered Markdown preview.

The plan snapshots this file's bytes before firing those commands and compares
after — so the exact content here does not matter, only that it is stable
Markdown that renders into a preview. Do not add a trailing-whitespace or
newline quirk that a save might normalize; nothing here is saved during the plan.

- one
- two
- three
