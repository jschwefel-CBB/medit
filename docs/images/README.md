# Documentation images

Screenshots for the [README](../../README.md) and the
[User Manual](../MANUAL.md) live here.

Each image is referenced from a `<!-- SCREENSHOT: … -->` placeholder in the docs
that describes exactly what the shot should show (which file open, which panel
visible, what to highlight). Capture them from the running app at a consistent
window size, light appearance unless a dark-mode shot is called for, and a Retina
(2×) scale.

Filenames referenced by the docs (fill these in):

- `hero.png` — hero shot (code file, sidebar, highlighting, status bar)
- `first-launch.png`, `window-anatomy.png`
- `editor.png`, `rainbow-brackets.png`
- `find-replace.png`, `find-in-tabs.png`
- `tabs.png`
- `text-menu.png`, `block-edit.png`
- `markdown-preview.png`, `markdown-toolbar.png`
- `sidebar.png`, `recent-files.png`
- `status-bar.png`, `language-popup.png`, `encoding-menu.png`, `reload-banner.png`
- `settings.png`
