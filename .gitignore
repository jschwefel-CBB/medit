# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Xcode — user-specific
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout

# Xcode — build
build/
DerivedData/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

# Swift Package Manager
.swiftpm/
.build/
Packages/
# Package.resolved is intentionally committed (this is an app, not a library)
# so the exact resolved Highlightr version is reproducible.

# Obj-C / Swift
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Misc
*.swp
*~.nib

# Icon generator artifacts (regenerable via Tools/IconGen/iconmaker.swift)
/Tools/IconGen/iconmaker
/Tools/IconGen/iconset_out/
/Tools/IconGen/previews/

# NEVER reintroduce this directory. Design docs live in docs/specs/ and
# docs/plans/. Any 'superpowers' path is ignored so it can never be committed.
**/superpowers/
superpowers/
